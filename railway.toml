[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "npm run build && npm run migration:run && npm run start:prod"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3